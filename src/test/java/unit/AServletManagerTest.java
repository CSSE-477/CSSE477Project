package unit;

import org.junit.Before;
import org.junit.Test;

import servlet.AServletManager;

import static org.junit.Assert.assertEquals;

public class AServletManagerTest {

    @Before
    public void setUp() {
        // TODO: Create a config file in the proper location
        // TODO: Create the Concrete ServletManager
    }

    @Test
    public void testGetHeaders() throws Exception {
        assertEquals(true, true);
    }

    // TODO: Specify an inner class that inherits from AServletManager
}